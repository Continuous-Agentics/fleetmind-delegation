# `@continuous-agentics/delegation-core`

Versioned, channel-neutral task, lifecycle, and NATS contracts shared by FleetMind and the OpenClaw delegation plugin.

This package intentionally contains no DynamoDB client, NATS client, or channel API implementation. Those belong to adapters.
